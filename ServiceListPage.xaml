<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QR_scanner_zxing.ServiceListPage"
             Title="Serviços do dispositivo">
    <VerticalStackLayout>
        <CollectionView x:Name="servicesCollectionView" SelectionMode="Single" SelectionChanged="OnServiceSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Name}" FontSize="16"/>
                        <Label Text="{Binding Id}" FontSize="12" TextColor="Gray"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>